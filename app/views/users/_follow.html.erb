<%= form_with(model: current_user.active_relationships.build, local: true, id: "follow_form") do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "友達追加", class: "btn btn-main-green btn-user_path" %>
<% end %>